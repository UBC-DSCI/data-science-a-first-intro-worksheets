name: base
channels:
  - conda-forge
dependencies:
  - ". . Hello readers! This is a message from the authors of 'Data Science: A First Introduction'. Unfortunately the `conda env update --file ...` command that we provided in the 1st edition of the book no longer works properly for all operating systems. So instead, if you are on either Mac or Ubuntu, ... [TODO]`
